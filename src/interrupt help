interrupt help

global
;; extern void* in c
make asm with 256 entries

isr0:
    sub if no error
    push isr num to arg registers
    jmp to general command
isr1:


general:
    save volatile
    call c command
    pop volatile 
    add error
    iretq
....